    // Count total moves
    let total_moves = 0;

    // Clicked cards array
    let clicked_cards = [];

    let userName;

    // Points to Win
    // const points_to_win = document.querySelectorAll('img.toy-avatar').length
    let timer;
    let points = 0;


